# J.A.R.V.I.S. CLI

The CLI is the first runnable interface for the project.

Run locally:

```bash
python3 apps/cli/main.py
```

The first version uses the mock model provider by default so the assistant can run without external API keys.
